{% extends 'base.html.twig' %}

{% block title %}Instances{% endblock %}

{% block body %}
    <h1>Instances</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instance.id }}</td>
            </tr>
            <tr>
                <th>NameInstance</th>
                <td>{{ instance.nameInstance }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-center">
        <a class="btn btn-outline-primary" href="{{ path('app_admin') }}">back to list</a>
        <a class="btn btn-outline-primary" href="{{ path('app_instances_edit', {'id': instance.id}) }}">edit</a>
    </div>
    

    {{ include('instances/_delete_form.html.twig') }}
{% endblock %}
