<form method="post" action="{{ path('app_boss_delete', {'id': boss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boss.id) }}">
    <button class="btn">Delete</button>
</form>
