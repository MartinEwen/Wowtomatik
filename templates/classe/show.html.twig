{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ classe.picture }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-center">
        <a class="btn btn-outline-primary" href="{{ path('app_admin') }}">back to list</a>
        <a class="btn btn-outline-primary" href="{{ path('app_classe_edit', {'id': classe.id}) }}">edit</a>
    </div>
    

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
