<form method="post" action="{{ path('app_kill_delete', {'id': kill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kill.id) }}">
    <button class="btn">Delete</button>
</form>
