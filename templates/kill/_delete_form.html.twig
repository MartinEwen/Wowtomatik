<form method="post" action="{{ path('app_kill_delete', {'id': kill.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kill.id) }}">
    <button class="btn">Delete</button>
</form>
